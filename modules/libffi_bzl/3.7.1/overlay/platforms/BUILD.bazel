"""The libffi-specific host cases of configure.host, as config settings."""

load("@bazel_skylib//lib:selects.bzl", "selects")

package(default_visibility = ["//:__pkg__"])

selects.config_setting_group(
    name = "x86_32_not_macos",
    match_any = [
        "@platforms_extended//platform:x86_linux",
        "@platforms_extended//platform:x86_windows",
        "@platforms_extended//platform:x86_bsd",
    ],
)

selects.config_setting_group(
    name = "arm32_not_macos",
    match_any = [
        "@platforms_extended//platform:arm32_linux",
        "@platforms_extended//platform:arm32_windows",
        "@platforms_extended//platform:arm32_bsd",
    ],
)

# i?86 freebsd and openbsd get their own target; netbsd is not special-cased
# upstream and falls through to i?86-*-*.
selects.config_setting_group(
    name = "x86_freebsd",
    match_all = [
        ":x86_32_not_macos",
        ":freebsd_or_openbsd",
    ],
)

selects.config_setting_group(
    name = "freebsd_or_openbsd",
    match_any = [
        "@platforms//os:freebsd",
        "@platforms//os:openbsd",
    ],
)

selects.config_setting_group(
    name = "gnu_compiler",
    match_any = [
        "@rules_cc//cc/compiler:gcc",
        "@rules_cc//cc/compiler:clang",
    ],
)
